# Changelog

このプロジェクトの主な変更点をまとめています。バージョン番号は `manifest.json` の `Version` に対応します。

## v1.0.0 (最新・公開版)

- **CodeQL「Prototype-polluting function」の根本的な再修正**: 2回のガード追加(オブジェクトハッシュ方式・配列indexOf方式)を行ったが、CodeQLの再スキャンでも解消されなかった(「再帰的なチェーン代入」という構造そのものを検出パターンとしているためと考えられる、Material-UI等でも報告のある既知のパターン)。実際の呼び出しが常に「グループ名.固定フィールド名」の2階層のみだったことから、可変長ドットパスをループで辿る汎用実装自体をやめ、`writeSetting(group, field, value)`という**ループ・再帰を伴わない1階層のみの代入**に構造変更。全13箇所の呼び出し元を新しい引数形式に更新し、正常系・攻撃防御・未初期化グループへの書き込み防止の全パターンをテストで確認
- **GitHub ActionsをNode.js 24対応版に更新**: `actions/checkout@v4→v5`、`actions/setup-node@v4→v5`、`actions/upload-artifact@v4→v6`に更新。あわせて、ビルドに使うNode.jsのバージョン指定も`"20"→"24"`に変更(Node.js 20は2026年4月30日に既にEOLを迎えており、GitHub Actionsのランナーからも2026年9月16日に完全に削除される予定だったため)。`softprops/action-gh-release@v2`はNode 24対応状況が未確認のため、今回は変更を見送り
- **セキュリティ対応(CodeQL指摘への対応)**:
  - `pi-common.js`の`populateSelect`関数で、デバイスID/シーンID(`item[idKey]`・`currentId`)がHTML属性値に未エスケープで挿入されていた箇所を`escapeHtml()`で保護(CodeQL: DOM text reinterpreted as HTML / Client-side cross-site scripting、High×2件)
  - `control.html`の`writeSetting`関数（ドット区切りパスでの動的プロパティ書き込み）に、`__proto__`/`constructor`/`prototype`への書き込みを防ぐガードを追加(CodeQL: Prototype-polluting function、Medium×1件)
  - `.github/workflows/build.yml`に`permissions: contents: write`を明記、`SECURITY.md`を追加(CodeQLの「Workflow does not contain permissions」警告解消・脆弱性報告窓口の明示)
- **英語版の「開発者向け情報」リンクを復元**: 前回の手直しで誤って削除されていたのを修正(日本語版には残っていたため、両言語間で不整合が生じていた)

- **README再手直し**(作者による追加修正を反映): 機能一覧の対応表で、標準4アクションもStream Deck+列に✓を表示するよう変更(Stream Deck+はStream Deckの上位互換のため技術的に正しい表記)。細かい言い回しの調整、「Air Conditioner Control」の英語表記を日本語版内で「エアコンコントロール」に統一

- **日本語版の訳語を見直し**(作者による手直しを反映): アクション名を英語併記から日本語名に統一、対応表を○表記に統一等。手直しの過程で見つかった見出し重複バグ(「Triple Actionの設定画面」が誤って「トグルアクション」と重複していた点)を修正。対応デバイス表への「フロアライト」追加は、実コード側(`DEVICE_COMMAND_MAP`)の`deviceType`文字列が未確認のため見送り
- **インストール手順に開発者向け情報へのリンクを追加**(両言語): 「一般ユーザー向け」の手順の最後に、ソースからビルドしたい人向けの導線を設置

- **GitHubリポジトリ情報を確定**: `https://github.com/DOYA09/switchbot-controller` に合わせて、READMEの`git clone`コマンド、`manifest.json`の`URL`欄、`package.json`の`repository`/`homepage`/`bugs`欄(新規追加)を更新
- **8言語のPI画面ヒント文言を修正**: 「開発者向けオプション」取得時のバージョンタップ回数について、実機で確認できていない「10回」という具体的回数の記載を、全8言語で「連続タップ」に統一(`en.json`は既に修正済みだったが、同じ文言を持つ`pi-common.js`側の8言語が未修正だったため対応)
- **設定画面スクリーンショットを追加**: トグルアクション・トリプルアクション・センサー表示の3画面を新規追加。シングルアクションの3画面は新版に差し替え。センサー表示は実機クローズアップ写真も追加。パラメーターコントロールのみ、対応デバイス未所有のため引き続き未掲載
- **英語表記の修正**: 「Tape Light」を、SwitchBot公式の英語表記に合わせて「Strip Light」に統一(README.md、`en.json`、`tapelight.html`のページタイトル、README.ja.md内の英語alt属性・見出し表記を含む、計18箇所)。日本語表記(テープライト)は変更なし
- **PV・ヒーローバナーの差し替え**: 英語表記のミスを修正した最新版に差し替え。動画は39MB→約2.9MBに圧縮(93%軽量化)、サムネイルも再生成
- **デモ動画(25秒)をREADME冒頭に追加**(サムネイル画像をクリックすると再生。`docs/video/demo.mp4`)
- **ヒーローバナーを差し替え**(フォント等を調整した最新版に更新。PNG→JPEG化で約77%軽量化)
- **日本語版の用語を平易化**: 「Property Inspector」「PI」の表記を全て「設定画面」に統一(既存の「エアコン詳細設定」というコマンド名との混同を避けるため、「詳細設定」ではなく「設定画面」を採用)。英語版は`Property Inspector`のまま維持(Elgato公式の標準用語のため)

- **READMEの構成を全面的に見直し**(ChatGPTによる改訂案から、良いと判断した部分のみを反映):
  - 「開発者向け情報」をインストール手順から完全に分離し、文書の最後に集約(一般ユーザーが開発者向けの内容を読み飛ばしやすいように)
  - 実機写真を機能一覧の直後・インストール手順より前に移動(早い段階で「実際に動いている証拠」を見せる構成に)
  - 実機写真とPI設定画面のスクリーンショットを別セクションに分離
  - 「対応環境」の独立セクションを新設(Stream Deck / Stream Deck +それぞれで使える機能を明確化)
  - 冒頭に「最新版をダウンロード / 不具合を報告 / 機能をリクエスト」のクイックリンクを追加
  - 「開発者向け情報」の冒頭に「使うだけなら読み飛ばしてOK」という一言を追加
  - 目次・「従来のAPI連携 vs 本プラグイン」比較表・Stream Deck対応表の表記は変更せず維持

- READMEのヒーローバナーを日英それぞれ更新(`hero-banner.png`=日本語版、`hero-banner-en.png`=英語版として分離。日本語版はレイアウトも微調整)
- **表記統一**: `manifest.json`の`Name`/`Category`（Stream Deck上に実際に表示される名称）を含め、プロジェクト全体で「Switch Bot」（誤）→「SwitchBot」（正）に統一。日本語版READMEの「Token」「Client Secret」（誤）→「トークン」「クライアントシークレット」（正）の表記ブレも解消
- **READMEを英語(メイン)・日本語(`README.ja.md`)のバイリンガル構成に変更**。Marketplace公開を見据え、英語版をメインのREADME.mdとした
- 冒頭のキャッチコピーを刷新(「SwitchBotをStream Deckから、もっと直感的に」)
- 「SwitchBot Controllerとは？」セクションに、従来のAPI連携(5ステップ)と本プラグイン(3ステップ)を比較する表を追加し、導入の手軽さを訴求
- 「3分で始めるSwitchBot Controller」という5ステップのクイックスタートを新設し、機能一覧の直後・詳細なインストール手順の前に配置
- インストールセクションを機能一覧の直後に移動(旧: 実機写真の後)
- SwitchBot APIトークン取得手順に、実際のSwitchBotアプリの画面キャプチャ(黒塗り済み)を追加
- READMEのヒーローバナーを、作者自作版に差し替え(実機写真・実際のアイコンを使用し、AI生成バナーにあった誤表記等を修正したもの)
- READMEを全面刷新（実機写真・設定画面スクリーンショット・512pxアイコンを追加、目次・バッジ・GitHub Flavored Markdownの通知ボックスを整備）
- 実機写真・スクリーンショットに写り込んでいたSwitchBot APIトークンを検出し、黒塗りで除去
- バージョンを公開版として`1.0.0`に変更

## v1.24.0

- ロゴ・全8アクションアイコンをブリックレッド+グレー(トグルアクションと同色)の2色、統一された線の太さで再デザイン
- `operation-runner.ts`(全アクション共通の実行ロジック)・`switchbot-api.ts`の`isRateLimitError`にユニットテストを追加
- **レート制限専用のエラー表示**を追加。SwitchBot APIのレート制限(HTTP 429 / 日次上限到達)を検知し、対応しているアクション(ダイヤル系・センサー表示)では「通信エラー」ではなく「レート制限」と表示を区別(`src/switchbot-api.ts`の`isRateLimitError`/`RateLimitError`/`throwIfFailed`)
- 型安全性の監査(`keyof` を使ったループでの文字列メソッド呼び出しがないか等)。追加の問題は見つからず
- 未使用の翻訳キー(`paramdial.opt.ac`)を削除

## エアコン関連機能(v1.19.0 〜 v1.26.x)

- **エアコンコントロール**アクションを新規追加(パラメーターコントロールから分離)
  - ダイヤル回転で設定温度を調整(摂氏16-30 / 華氏60-90)
  - ダイヤル押し込みで電源オン/オフ
  - タッチで有効なモード(冷房/暖房/除湿/送風)を順番に切替、冷房/暖房はそれぞれのデフォルト温度を適用
  - 表示単位(℃/℉)を切替可能。華氏選択時のデフォルト温度は78℉
- シングル/トグル/トリプルアクション・パラメーターコントロールのタッチ設定に、エアコン向けコマンドを追加
  - **エアコン詳細設定**: モード・温度(℃/℉切替可能)・風量を個別設定し、実行時は常に電源オンで送信
  - **エアコン温度調整**: 押すたびに設定温度を1℃ずつ変更(上げる/下げる)
  - 上記2つに加えて、通常のオン/オフ/トグルもエアコンで選択可能に
- デバイスID単位で温度/モード/風量/電源の状態を共有するグローバル状態(`src/ac-state.ts`)を導入し、エアコンコントロールと各種アクションのエアコン操作が相互に反映されるように
  - 自分の操作直後(既定4秒)は自動反映を止める「クワイエット期間」により、操作直後に古い値で表示が巻き戻る不具合を回避
- 明るさ・色温度に対応するデバイス向けに「明るさを調整」「色温度を調整」コマンドを追加(押すたびに実機の現在値を取得して±1段階)

## シーンライブラリ・パラメーターコントロール・テープライトコントロールの拡張(v1.14.0 〜 v1.18.x)

- シーンライブラリ: 登録シーン一覧の表示・並べ替え(ドラッグ&ドロップ)・表示/非表示切替・文字サイズ調整・手動更新ボタンを追加
- パラメーターコントロール: タッチスクリーンに最大3つの操作(コマンド or シーン)を割り当て可能に
- テープライトコントロール: 操作対象(明るさ/色温度/カラー(RGB)/カスタム)を選択可能に。カラーはRGB値を15刻みで変化させる虹色サイクル(全102ステップ)に対応
- 各種UIの一貫性向上(コマンド変更時のパラメーター自動リセット、デバイス変更時のコマンド自動リセット、入力欄の幅統一など)

## 公開に向けた基盤整備(v1.10.0 前後)

- MITライセンス、非公式である旨の免責事項をREADMEに明記
- `@elgato/streamdeck` のバージョン固定
- `crash.log` のローテーション、詳細ログのデバッグレベル化
- Smart Lock(施錠/解錠)デバイスの対応
- 英語をはじめ8言語(日本語・英語・ドイツ語・フランス語・スペイン語・韓国語・中国語簡体字/繁体字)のローカライズ対応
- 同一デバイスへの重複ポーリングを避ける共有キャッシュ(`src/device-status-cache.ts`)
- ESLint/Prettier、Vitestによるユニットテスト、GitHub Actionsでのビルド自動化を導入

## 初期リリース

- シングル/トグル/トリプルアクション、センサー表示、Tapelight Control(現テープライトコントロール)の実装
- SwitchBot OpenAPI v1.1(HMAC-SHA256認証)を用いたデバイス操作・シーン実行
